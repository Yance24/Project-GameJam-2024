using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestCursorRotation : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        // transform.eulerAngles = YEuler.countAngle(transform.position,(Vector2) Camera.main.ScreenToWorldPoint(Input.mousePosition));
        
    }
}
